<!DOCTYPE html>
<html>
<head>
  <title>MailOffice</title>
  <link rel="shortcut icon" href="/assets/favicon.ico">
  <%= stylesheet_link_tag "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>

<body>

<% if user_signed_in? %>
  <div class="navbar navbar-inverse navbar-fixed-top">
    <div class="navbar-inner">
      <div class="container-fluid">
        <button type="button" class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        <a class="brand" href="<%= letters_path %>">BagelMail</a>

        <div class="nav-collapse collapse">
          <p class="navbar-text pull-right">
            Connecté en tant que <a href="<%= edit_registration_path(current_user) %>" class="navbar-link"><%= current_user.email %></a>
            |
            <%= link_to('<i class="icon-off"></i>'.html_safe, destroy_user_session_path, :method => :delete) %>
          </p>
          <ul class="nav">
            <li class="<%= menu_active 'letters' %>"><%= link_to 'Courriers', letters_path %></li>
            <li class="<%= menu_active 'subscription' %>"><%= link_to 'Abonnement', subscription_path %></li>
            <li class="<%= menu_active 'account' %>"><%= link_to 'Mon Compte', edit_registration_path(current_user) %></li>
            <li class="<%= menu_active 'help' %>"><%= link_to 'Aide', '#' %></li>
          </ul>
        </div>
        <!--/.nav-collapse -->
      </div>
    </div>
  </div>
<% else %>
<header id="header" class="site-header">
  <div class="container">
    <div class="row-fluid">
      <div class="span12">
        <h1><a class="site-title pull-left" href="/"><i class="icon-envelope"></i> MailOffice.me</a></h1>
      </div><!-- /span12 -->
    </div><!-- /row-fluid -->
  </div><!-- /container -->
</header>
<% end %>

<div class="container" style="margin-top: 70px">
  <%= render partial: 'shared/flash_messages' %>
  <%= yield %>
</div>

</body>
</html>
